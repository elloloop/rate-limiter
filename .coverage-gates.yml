# Per-package coverage floors. Thresholds sit a few points under current
# coverage so they act as a regression ratchet: a change that drops a
# package below its floor fails CI. Raise a floor whenever coverage climbs
# comfortably above it.
default: 60

include:
  - internal/
  - cmd/
  - ratelimiterserver

packages:
  cmd/quota-service: 80
  internal/config: 100
  internal/events: 75
  internal/keys: 100
  internal/limits: 95
  internal/metrics: 95
  ratelimiterserver: 92
  ratelimiterserver/backend: 87
  ratelimiterserver/backend/redis: 88
